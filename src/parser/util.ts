// // import parser from "./parser.js";

// import { RequestOptions } from "node:https";
// import { type } from "node:os";

// // export function getCmd<T extends parser.Type, C extends parser.Command>(d: parser.Direction, t: T): C | undefined {

// // }
// type Razor<T> = {
//     [P in keyof T]: (c: P) => string;
// }
// class RazorII implements Razor<RequestOptions> {
// }
// const x = new RazorII().servername(true);